# treehole image proxy

a image proxy from chevereto api to lsky api

## usage

```shell
# build for release
go build -tags "release" -ldflags "-s -w" -o treehole.exe
# run
./treehole.exe
```
